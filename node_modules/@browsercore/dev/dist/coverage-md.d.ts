/**
 * Pure rendering logic for the coverage-md bin, extracted from bin/coverage-md.mjs
 * so it can be unit-tested without touching the filesystem. The bin imports these
 * helpers and handles I/O; everything here is deterministic given a coverage summary.
 */
export interface MetricTotals {
    total: number;
    covered: number;
    pct: number;
}
export interface CoverageSummary {
    total: {
        statements: MetricTotals;
        branches: MetricTotals;
        functions: MetricTotals;
        lines: MetricTotals;
    };
}
export interface Metric {
    label: string;
    key: keyof CoverageSummary["total"];
}
export declare const METRICS: Metric[];
export declare const renderMetric: (m: MetricTotals) => string;
export declare const badgeColor: (pct: number) => string;
export declare const renderCoverageMarkdown: (data: CoverageSummary, fileEntries: Array<{
    file: string;
    m: CoverageSummary["total"];
}>) => string;
export declare const renderBadge: (statementsPct: number) => {
    schemaVersion: number;
    label: string;
    message: string;
    color: string;
    namedLogo: string;
};
//# sourceMappingURL=coverage-md.d.ts.map